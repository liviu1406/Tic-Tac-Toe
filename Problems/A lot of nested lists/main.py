n = int(input())

my_list = [[i for i in range(1, 3)] for n in range(n)]
print(my_list)


# # the following line reads the list from the input, do not modify it, please
# old_list = [int(num) for num in input().split()]
#
# binary_list = [1 if num > 0 else 0 for num in old_list]
# print(binary_list)
#
# #  new_list = [num if num >= 0 else 0 for num in old_list]

# matrix = [[j for j in range(5)] for i in range(2)]
